services:
  app:
    restart: always
    build: .
    ports:
      - "80:8000"
    env_file:
      - .env.dev
    volumes:
      - .:/app
    working_dir: /app/backend
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000